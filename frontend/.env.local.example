BACKEND_INTERNAL_BASE_URL=http://127.0.0.1:8080
PUBLIC_APP_ORIGIN=http://localhost:3000
GITHUB_CLIENT_ID=replace-with-github-oauth-client-id
GITHUB_CLIENT_SECRET=replace-with-github-oauth-client-secret
# Optional callback declaration. Set this with
# GITHUB_OAUTH_REQUIRE_CALLBACK_MATCH=true in non-production environments.
# GITHUB_OAUTH_CALLBACK_URL=http://localhost:3000/api/auth/github/callback
# GITHUB_OAUTH_REQUIRE_CALLBACK_MATCH=false
AUTH_SESSION_SECRET=replace-with-long-random-session-secret
ANTI_ABUSE_HMAC_SECRET=replace-with-long-random-anti-abuse-secret
TRUST_PROXY_HEADERS=false
# Which forwarded header carries the edge-derived client address. Its name is
# validated at startup and the header is stripped from every proxied request
# regardless of TRUST_PROXY_HEADERS; the flag decides only whether the value
# is trusted as the client address. Defaults to x-envoy-external-address,
# which is what the AKS Envoy Gateway publishes. Set it only when a different
# Layer-7 proxy terminates the connection AND overwrites the header for
# untrusted peers -- x-real-ip behind k3s Traefik, cf-connecting-ip behind
# Cloudflare.
# TRUSTED_CLIENT_IP_HEADER=x-envoy-external-address
NEXT_PUBLIC_TURNSTILE_SITE_KEY=replace-with-turnstile-site-key
# Optional browser Sentry runtime config
# NEXT_PUBLIC_SENTRY_ENABLED=false
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_SENTRY_ENVIRONMENT=development
# NEXT_PUBLIC_SENTRY_REPLAY_SESSION_SAMPLE_RATE=0
# NEXT_PUBLIC_SENTRY_REPLAY_ON_ERROR_SAMPLE_RATE=0
# NEXT_PUBLIC_ADMIN_ANALYTICS_ENABLED=false
# Google Cloud region where Claude is available on Vertex AI
# Common regions: us-east5, us-central1, europe-west1
CLOUD_ML_REGION=us-east5

# Your GCP project ID (not project number)
ANTHROPIC_VERTEX_PROJECT_ID=your-gcp-project-id
