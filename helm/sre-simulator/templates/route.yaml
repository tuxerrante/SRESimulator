{{- if eq (include "sre-simulator.exposureMode" .) "route" }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "sre-simulator.fullname" . }}
  labels:
    {{- include "sre-simulator.labels" . | nindent 4 }}
spec:
  host: {{ include "sre-simulator.publicHost" . }}
  tls:
    termination: {{ .Values.route.tls.termination }}
  to:
    kind: Service
    name: {{ include "sre-simulator.fullname" . }}-frontend
    weight: 100
  port:
    targetPort: {{ .Values.frontend.port }}
{{- end }}
